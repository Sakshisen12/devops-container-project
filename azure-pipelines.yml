trigger:
- main

pool:
  name: 'Default'

steps:
- task: Docker@2
  displayName: 'Build and Push Image to ACR'
  inputs:
    containerRegistry: 'Azure-Cloud-Connection'
    repository: 'devops-container-project'
    command: 'buildAndPush'
    Dockerfile: '**/Dockerfile'
    tags: 'latest'
